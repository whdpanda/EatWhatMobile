import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  empty: {
    marginTop: 10,
    textAlign: 'center',
    color: '#bbb',
    fontSize: 15,
  },
  error: {
    color: 'red',
    marginTop: 10,
    textAlign: 'center',
    fontSize: 15,
  },
  loading: {
    marginTop: 20,
  },
    contentContainer: {
    paddingBottom: 20,
  },
});
